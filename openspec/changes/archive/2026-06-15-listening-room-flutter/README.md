# listening-room-flutter

Implements the Listening Room (music sync) feature inside the native Flutter client. This includes creating a `RoomProvider` to handle API interactions, subscribing to real-time updates via SSE stream, and building a clean, native Flutter UI screen for room actions (create, join, leave) while preserving the app's current styling.
