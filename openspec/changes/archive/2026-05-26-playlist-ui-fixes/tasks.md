## 1. Logic Fixes

- [x] 1.1 Inspect and update `playlist_provider.dart` or `audio_handler.dart` to fix shuffle behavior so that shuffling properly shuffles the queue and playback starts at the first song of the shuffled list.

## 2. UI Adjustments

- [x] 2.1 Remove the Play button from the playlist header view.
- [x] 2.2 Add padding to the top of the playlist content so it's not hidden under the top navigation bar.
- [x] 2.3 Add visual padding between the Shuffle button and the first song in the list.
