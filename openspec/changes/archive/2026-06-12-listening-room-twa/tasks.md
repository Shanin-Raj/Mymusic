# Tasks - Listening Room TWA

- [x] Backend: Implement room endpoints (`/api/rooms`, `/api/time`, `/api/songs/:id`, and SSE stream `/api/rooms/:roomId/stream`) in `backend/server.js`
- [x] CSS: Add Listening Room styling and breathing pulsing dot animation in `backend/public/styles.css`
- [x] HTML: Add Room entry button and screen wrapper `div#screen-room` in `backend/public/index.html`
- [x] JS: Implement clock syncing, room actions (create, join, leave, copy), SSE connection, and playback synchronization in `backend/public/app.js`
- [x] Verification: Create integration test `verify_endpoints.js` and verify all endpoints succeed
- [x] Git: Commit changes and push to GitHub
