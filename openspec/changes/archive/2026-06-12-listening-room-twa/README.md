# listening-room-twa

Implements the Listening Room (music sync) feature in the Web App / TWA codebase of the Mixtape application. This includes creating backend Express routes to manage Firestore room documents, streaming updates via Server-Sent Events (SSE), and creating the HTML/CSS/JS client-side implementation.
